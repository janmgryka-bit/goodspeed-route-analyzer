--- a/jnius/jnius_utils.pxi
+++ b/jnius/jnius_utils.pxi
@@ -319,7 +319,7 @@
 
         if r == 'S' or r == 'I':
             if isinstance(arg, int) or (
-                    (isinstance(arg, long) and arg < 2147483648)):
+                    False):  # Python 3: long type doesn't exist, int handles all values
                 score += 10
                 continue
             elif isinstance(arg, float):
@@ -330,7 +330,7 @@
                 return -1
 
         if r == 'J':
-            if isinstance(arg, int) or isinstance(arg, long):
+            if isinstance(arg, int):  # Python 3: long type doesn't exist
                 score += 10
                 continue
             elif isinstance(arg, float):
